# Relay server — Strands Agents backend
#
# The [anthropic,openai] extras are installed so Relay runs before Bedrock model
# access is switched on. Once Bedrock is enabled, no reinstall is needed — the
# base package already ships the Bedrock provider; just set LLM_PROVIDER=bedrock.
strands-agents[anthropic,openai]
strands-agents-tools

fastapi
uvicorn[standard]
pydantic
python-dotenv

# AgentCore Memory. Relay degrades to a local JSON store when
# AGENTCORE_MEMORY_ID is unset or credentials are missing, so the demo never
# hard-fails on it.
bedrock-agentcore
